<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de Usuario</title>
</head>
<body>
    <div>
        <nav>
            <ul>
                <li><a href="/products/carts/{{cart}}">Carrito</a></li>
            </ul>
        </nav>
        <h1>Perfil de Usuario</h1>
        
        {{#if profileImage}}
            <img src="{{profileImage}}" alt="Foto de perfil" style="width: 150px; height: 150px;">
        {{else}}
            <p>No hay foto de perfil</p>
        {{/if}}
        
        <p>Nombre: {{first_name}}</p>
        <p>Apellido: {{last_name}}</p>
        <p>Correo electrónico: {{email}}</p>
        <p>Edad: {{age}}</p>
        <p>Última conexión: {{last_connection}}</p> 

        <form action="/logout" method="GET">
            <button type="submit">Logout</button>
        </form>

        <form action="/api/users/:uid/documents" method="POST" enctype="multipart/form-data">
            <input type="file" name="document">
            <button type="submit">Subir Documento</button>
        </form>
    </div>
</body>
</html>
