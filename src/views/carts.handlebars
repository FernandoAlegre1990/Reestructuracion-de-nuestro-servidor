<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .cart-container {
            margin: 20px auto;
            max-width: 800px;
            color: white;
        }
        .cart-header {
            margin-bottom: 20px;
        }
        .cart-header span {
            font-size: 1.2rem;
            font-weight: bold;
            color: white;
        }
        .product-list {
            list-style: none;
            padding: 0;
        }
        .product-item {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            background-color: #f8f9fa;
            padding: 10px;
            border-radius: 8px;
            color: black;
            min-height: 150px; /* Ensure consistent height */
        }
        .product-item img {
            max-width: 100px;
            margin-right: 20px;
        }
        .product-item span {
            font-size: 1rem;
            color: #343a40;
        }
        .btn-container {
            margin-top: 20px;
        }
        .ticket {
            margin-top: 20px;
            display: none;
        }
        .ticket h4 {
            font-weight: bold;
        }
        .ticket ul {
            list-style: none;
            padding: 0;
        }
        h4 {
            color: white;
        }
        body {
            background-color: #343a40;
        }
    </style>
</head>
<body>
    <div class="container cart-container">
        <div class="cart-header">
            <span>ID del Carrito: {{cid}}</span>
        </div>

        <div>
            <h4>Productos agregados al carrito</h4>
            <ul class="product-list">
                {{#each products}}
                <li class="product-item">
                    <img src="{{thumbnail}}" alt="{{title}}">
                    <span>Producto: {{title}} - ${{price}} - Cantidad: {{quantity}}</span>
                </li>
                {{/each}}
            </ul>
        </div>

        {{#if products.length}}
        <div class="btn-container">
            <button id="purchaseBtn" class="btn btn-primary" data-cart-id="{{cid}}" data-mail-user="{{mailUser}}">Finalizar Compra</button>
            <button id="emptyCartBtn" class="btn btn-danger" data-cart-id="{{cid}}">Vaciar Carrito</button>
        </div>
        {{/if}}

        <div id="purchaseResults" class="ticket">
            <h4>Productos Comprados</h4>
            <ul id="purchasedProducts"></ul>
            <h4>Productos No Comprados</h4>
            <ul id="notPurchasedProducts"></ul>
            <p id="totalCuenta"></p>
        </div>
    </div>

    <!-- Scripts al final de la página -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/cart.js"></script>
    <script src="/js/cartActions.js"></script>
    <script src="/js/cartPurchase.js"></script>
</body>
</html>
